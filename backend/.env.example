# See ../.env.example (single source of truth)
# Keep SQLite for local development, or set a Supabase/PostgreSQL URL.
DATABASE_URL=sqlite:///specmatch.db
# Example: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
